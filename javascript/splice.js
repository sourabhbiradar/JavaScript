document.getElementById("topic").innerHTML="splice"

let ogArr=['1','2','3',4]

splc = ogArr.splice(-1)

console.log(splc,ogArr)

document.writeln('slice creates copy of array but splice works and modifies the original array'+'</br>')
document.writeln('same methods as slice')

